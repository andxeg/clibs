В данном каталоге extensions и config файлы
для генерации сертификаторв максимально приближенных к
тем, что в заявке (см. каталог from_rt)

extensions and certificates fields, key length

Не добавлены cRLDistributedPoints and AuthorityInfoAccessSyntax
Это поля в extensions, в них хранятся адреса CRL списка и UFA-MCA01.crt

